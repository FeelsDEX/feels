# direnv configuration for Nix flake integration
# This automatically loads the Nix development environment when you cd into this directory
#
# Installation (if not already installed):
#   brew install direnv (macOS)
#   Then add to your shell rc file (~/.zshrc):
#     eval "$(direnv hook zsh)"
#
# After creating this file, run:
#   direnv allow

use flake